{
  "name": "flow-codeblock-mcp",
  "version": "0.2.0",
  "description": "Flow Codeblock stdio MCP Server for non-script code execution and script management.",
  "license": "MIT",
  "author": {
    "name": "renoelis"
  },
  "type": "module",
  "bin": {
    "flow-codeblock-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "build": "bun run scripts/build.ts",
    "check": "bun run scripts/build.ts",
    "start": "bun run dist/index.js",
    "prepack": "bun run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "4.1.12"
  }
}
